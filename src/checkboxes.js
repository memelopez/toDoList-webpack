//  Module: related to the status updates with completed checkboxes
import Store from './store';

const taskCompleted = (index) => {
  console.log('Index recieved at checkboxes.js : ' + index + ' --bye.');
};

export { taskCompleted };